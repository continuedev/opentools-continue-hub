name: Playwright MCP Server
version: 1.0.0
schema: v1

mcpServers:
  - name: Playwright
    command: npx
    args:
      - -y
      - "opentools@latest"
      - install
      - playwright-mcp-server
      - --client
      - continue
